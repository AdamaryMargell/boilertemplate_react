'use client';
import { Checkbox, CheckboxProps } from 'primereact/checkbox';

export const XCheckbox = ({
    ...props
}: CheckboxProps) => {

    return (
        <Checkbox
            {...props}

        />
    );
};