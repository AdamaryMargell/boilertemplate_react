import { classNames } from "primereact/utils";

export const toolbarPT = {
    root: {
        className: classNames('flex items-center justify-between flex-wrap', 'bg-gray-100  border border-gray-300  p-5 rounded-md gap-2')
    },
    start: 'flex items-center',
    center: 'flex items-center',
    end: 'flex items-center'
}